public interface iCompileLatex
{
    /// <summary>
    /// ✅ Checks if the conversion process has completed successfully.
    /// Returns true if a converted JSON exists, otherwise false.
    /// </summary>
    
}