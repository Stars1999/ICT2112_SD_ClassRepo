@page
@model ICT2106WebApp.Pages.QualityCheckerModel
@inject ICT2106WebApp.Interfaces.IPDFProvider PDFProvider
@{
    ViewData["Title"] = "PDF Quality Checker";
    // Get the report details through public methods
    var reportDetails = Model.Report.GetQualityReportDetails();
    var isSuccessful = reportDetails.Item1;
    var issues = reportDetails.Item2;
    var metrics = reportDetails.Item3;
    var qualityScore = reportDetails.Item4;
    
    // Check if we have PDF content in the provider
    var hasPdfInProvider = PDFProvider.GetPDFContent().Length > 0;
}

<h2>PDF Quality Checker</h2>

<!-- Upload Form -->
<form method="post" enctype="multipart/form-data" class="upload-form">
    <div class="filter-container">
        <label for="pdfFile">Upload PDF:</label>
        <input type="file" id="pdfFile" name="PdfFile" class="form-control" accept=".pdf" />
        <button type="submit" class="btn-primary">Check Quality</button>
    </div>
</form>

@if (hasPdfInProvider)
{
    <div class="provider-info">
        <p>A PDF is currently loaded in the provider.</p>
    </div>
}

<!-- Validation Message -->
@if (!ModelState.IsValid)
{
    <div class="alert alert-danger">
        @foreach (var error in ModelState.Values.SelectMany(v => v.Errors))
        {
            <p>@error.ErrorMessage</p>
        }
    </div>
}

@if (Model.ShowEmptyState)
{
    <div class="empty-state">
        <p>Please upload a PDF file to check its quality.</p>
        <p>The quality checker will analyze:</p>
        <ul>
            <li>Text formatting and font consistency</li>
            <li>Image resolution quality</li>
            <li>Document structure and organization</li>
            <li>Math notation formatting</li>
            <li>Bibliography and reference formatting</li>
        </ul>
    </div>
}
else if (Model.Report != null)
{
    <div class="report-container">
        <h3>Quality Report</h3>
        <div class="status-badge @(isSuccessful ? "success" : "error")">
            Status: @(isSuccessful ? "Passed" : "Issues Found")
        </div>

        <!-- Quality Score -->
        <p><strong>Quality Score:</strong> @qualityScore%</p>

        <!-- Issues List -->
        <h4>Issues Found</h4>
        <div class="issues-container">
            @if (issues.Any())
            {
                <ul class="issues-list">
                    @foreach (var issue in issues)
                    {
                        <li class="issue-item">@issue</li>
                    }
                </ul>
            }
            else
            {
                <p class="success-message">No issues found! The PDF meets all quality standards.</p>
            }
        </div>
    </div>
}

<p>
    Select a PDF file above and click the button to check its quality. The page will display a detailed report including an overall quality score.
</p>

<!-- Styles for Better UI -->
<style>
    .upload-form {
        margin-bottom: 20px;
    }

    .filter-container {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-bottom: 15px;
        align-items: center;
    }

    .form-control {
        padding: 6px;
        border: 1px solid #ddd;
        border-radius: 4px;
    }

    .report-container {
        background-color: #f8f9fa;
        border-radius: 5px;
        padding: 15px;
        margin-top: 20px;
        border: 1px solid #e9ecef;
    }

    .status-badge {
        display: inline-block;
        padding: 6px 12px;
        border-radius: 4px;
        font-weight: bold;
        margin-bottom: 15px;
    }

    .success {
        background-color: #28a745;
        color: white;
    }

    .error {
        background-color: #dc3545;
        color: white;
    }

    .table-container {
        overflow-x: auto;
        margin-bottom: 20px;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    th {
        background-color: #4A4A4A;
        color: white;
        padding: 12px;
        text-align: left;
    }

    td {
        padding: 10px;
        border: 1px solid #ddd;
    }

    tr:nth-child(even) {
        background-color: #f9f9f9;
    }

    .issues-container {
        background-color: #fff;
        border: 1px solid #ddd;
        border-radius: 4px;
        padding: 10px;
    }

    .issues-list {
        padding-left: 20px;
    }

    .issue-item {
        margin-bottom: 8px;
        color: #dc3545;
    }

    .success-message {
        color: #28a745;
        font-weight: bold;
    }

    .btn-primary {
        background-color: #007bff;
        color: white;
        padding: 8px 12px;
        border: none;
        cursor: pointer;
        border-radius: 5px;
    }

    .btn-primary:hover {
        opacity: 0.8;
    }

    .alert {
        padding: 12px;
        margin-bottom: 15px;
        border-radius: 4px;
    }

    .alert-danger {
        background-color: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
    }

    .empty-state {
        background-color: #f8f9fa;
        border: 1px dashed #ced4da;
        border-radius: 5px;
        padding: 20px;
        margin-top: 20px;
        text-align: center;
    }
    
    .empty-state ul {
        text-align: left;
        display: inline-block;
        margin: 15px auto;
    }
    
    .empty-state li {
        margin-bottom: 8px;
    }
</style>